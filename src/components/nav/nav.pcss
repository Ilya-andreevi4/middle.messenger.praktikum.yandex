.nav {
  position: absolute;
  gap: 16px;
  height: 32px;
  width: fit-content;
  top: 8px;
  right: 64px;
  display: flex;
  z-index: 3000;
}

.nav a {
  font-size: 16px;
  font-weight: lighter;
  color: inherit;
}