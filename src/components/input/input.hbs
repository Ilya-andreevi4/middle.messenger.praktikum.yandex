<input
  id={{id}}
  name={{id}}
  type="{{type}}"
  class="input {{className}}__input"
  {{#each attributes}}
    {{key}}="{{value}}"
  {{/each}}
  {{#if required}}
    required
  {{/if}}
  placeholder=" "
/>
