<input
  id={{id}}
  name={{name}}
  type="{{type}}"
  class="input {{className}}__input"
  {{#each attributes}}
    {{key}}="{{value}}"
  {{/each}}
  {{#if required}}
    required
  {{/if}}
  placeholder=" "
/>
