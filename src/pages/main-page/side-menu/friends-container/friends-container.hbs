<main class="friends-container">
  <header class="chats-header">
    <h3 class="title chats-header__title">
      Friends
    </h3>
    <div class="chats-header__input-box">
      <input
        class="input chats-header__input"
        type="text"
        name="search"
        id="search"
        placeholder="Search..."
      />
      {{{searchIcon}}}
    </div>
    {{{userIcon}}}
  </header>

  <ul class="chats-list">
    {{#each friends}}
      {{{ChatInfo
        id=id
        avatarSrc=avatar
        firstName=firstName
        lastName=lastName
        lastMessage=lastMessage
        className="chats-list"
        numberNewMessages=numberNewMessages
        time=time
        isActive=isActive
      }}}
    {{/each}}
  </ul>

  <header class="chats-header chats-header__groups">
    <h3 class="title chats-header__title">
      Groups
    </h3>
    {{{groupIcon}}}
  </header>

  <ul class="chats-list">
    {{#each groups}}
      {{{ChatInfo
        avatarSrc=this.avatar
        firstName=this.title
        lastMessage=this.lastMessage
        className="chats-list"
        numberNewMessages=this.numberNewMessages
        time=this.time
        isActive=isActive
      }}}
    {{/each}}
  </ul>
</main>
